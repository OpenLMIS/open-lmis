<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:mvc="http://www.springframework.org/schema/mvc"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd">

    <context:annotation-config/>
    <mvc:annotation-driven/>

    <context:component-scan base-package="org.openlmis.web.controller"/>
    <context:component-scan base-package="org.openlmis.admin.controller"/>
    <context:component-scan base-package="org.openlmis.core.controller"/>

    <http use-expressions="true">
        <intercept-url pattern="/js/*" access="permitAll" requires-channel="http"/>
        <intercept-url pattern="/bootstrap/*" access="permitAll" requires-channel="http"/>
        <intercept-url pattern="/auth/login" access="permitAll" requires-channel="http"/>
        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')" requires-channel="http"/>
        <intercept-url pattern="/logout" access="permitAll" requires-channel="http"/>

        <intercept-url pattern="/" access="isAuthenticated()" requires-channel="http"/>
        <intercept-url pattern="/**" access="hasRole('USER')" requires-channel="http"/>
        <form-login authentication-success-handler-ref="userAuthenticationSuccessHandler"
                login-processing-url="/j_spring_security_check" login-page="/auth/login"
                authentication-failure-url="/auth/login?error=true"/>
        <logout logout-success-url="/auth/login" invalidate-session="true" delete-cookies="true"/>
    </http>


    <authentication-manager>
        <authentication-provider ref="userAuthenticationProvider"/>
    </authentication-manager>


</beans:beans>